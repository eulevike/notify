services:
  - type: cron
    name: stock-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python monitor.py
    schedule: "2 * * * *"
    envVars:
      - key: ZAI_API_KEY
        sync: false
      - key: NTFY_TOPIC
        sync: false
